BASE_DIR=`pwd`

ext_dir ()
{
	for i in *.php; do
		(  echo $i 
	 	  awk '/\/\* function/,/\*\// {print $0;}' $i 
		) >> $BASE_DIR/function.txt
	done
}

cd ../html

ext_dir
cd $BASE_DIR

cd ../include

ext_dir

	
